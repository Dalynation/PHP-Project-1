
function reload(){
    window.location.reload();
}
// a function that cuases the page to reload
function pageLoading(){
setInterval(reload, 2000);
}
// after two second the reload function will activate
pageLoading();
// the page loading function is called
